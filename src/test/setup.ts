// import '@testing-library/jest-dom/vitest'; // Example: if you want jest-dom matchers
// Add any other global setup code for your tests here

// Example: Mock global 'fetch' if not using msw for all tests,
// or if msw isn't set up yet. For videoApi.test.ts, we'll mock authenticatedFetch directly.

// console.log('Vitest setup file loaded.');
